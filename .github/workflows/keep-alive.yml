name: Keep HF Space Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping HF Space
        run: |
          curl -L -s https://nv1023-genre-classifier.hf.space/ > /dev/null
